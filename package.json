{
  "name": "ztrain-tests",
  "version": "1.0.0",
  "description": "Tests Playwright pour le site Ztrain Web",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "^5.0.0"
  }
}
